/**
 * Debug CLI placeholder. The real debug CLI is implemented in Task 8.
 * This stub exists because the tsdown config declares src/cli.ts as its
 * second entry (thus emitting lib/cli.js) from scaffold time.
 */
export const cliName = 'dsh-browser-verify-debug-cli'
